name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  validate-version:
    name: Validate release version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check tag matches workspace version
        id: version
        shell: bash
        run: |
          version="$(
            python3 - <<'PY'
          import tomllib

          with open("Cargo.toml", "rb") as cargo_toml:
              manifest = tomllib.load(cargo_toml)

          print(manifest["workspace"]["package"]["version"])
          PY
          )"
          tag="${GITHUB_REF_NAME}"

          if [[ ! "$tag" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Release tags must use vMAJOR.MINOR.PATCH, got '$tag'" >&2
            exit 1
          fi

          if [[ "$tag" != "v${version}" ]]; then
            echo "Tag '$tag' does not match workspace version 'v${version}'" >&2
            exit 1
          fi

          echo "version=${version}" >> "$GITHUB_OUTPUT"

  test:
    name: Test workspace
    runs-on: ubuntu-latest
    needs: validate-version
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Test
        run: cargo test --workspace --all-targets

  build:
    name: Build ${{ matrix.target_name }}
    needs:
      - validate-version
      - test
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target_name: x86_64-unknown-linux-gnu
            archive: tar.gz
          - os: macos-15-intel
            target_name: x86_64-apple-darwin
            archive: tar.gz
          - os: macos-15
            target_name: aarch64-apple-darwin
            archive: tar.gz
          - os: windows-latest
            target_name: x86_64-pc-windows-msvc
            archive: zip
    runs-on: ${{ matrix.os }}
    env:
      VERSION: ${{ needs.validate-version.outputs.version }}
      TARGET_NAME: ${{ matrix.target_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build binaries
        run: cargo build --release --workspace --bins

      - name: Package Unix binaries
        if: runner.os != 'Windows'
        shell: bash
        run: |
          package="cli-tools-${VERSION}-${TARGET_NAME}"
          mkdir -p "dist/${package}"
          cp target/release/code-cost "dist/${package}/"
          cp target/release/dev-tools "dist/${package}/"
          cp target/release/git-tools "dist/${package}/"
          cp target/release/work-summary "dist/${package}/"
          cp target/release/zzz "dist/${package}/"
          tar -C dist -czf "${package}.tar.gz" "${package}"

      - name: Package Windows binaries
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $package = "cli-tools-$env:VERSION-$env:TARGET_NAME"
          New-Item -ItemType Directory -Path "dist/$package" | Out-Null
          Copy-Item target/release/code-cost.exe "dist/$package/"
          Copy-Item target/release/dev-tools.exe "dist/$package/"
          Copy-Item target/release/git-tools.exe "dist/$package/"
          Copy-Item target/release/work-summary.exe "dist/$package/"
          Copy-Item target/release/zzz.exe "dist/$package/"
          Compress-Archive -Path "dist/$package/*" -DestinationPath "$package.zip"

      - name: Upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: cli-tools-${{ env.VERSION }}-${{ matrix.target_name }}
          path: |
            cli-tools-${{ env.VERSION }}-${{ matrix.target_name }}.tar.gz
            cli-tools-${{ env.VERSION }}-${{ matrix.target_name }}.zip
          if-no-files-found: error

  release:
    name: Create GitHub Release
    needs:
      - validate-version
      - build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" artifacts/* \
            --title "${GITHUB_REF_NAME}" \
            --generate-notes \
            --verify-tag
